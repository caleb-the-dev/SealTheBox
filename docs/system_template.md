# System Name
*One-line description of what this system does.*

## Location
`scripts/path/to/file.gd` (autoload / scene node / resource)

## Responsibility
What this system owns. What it does NOT own (boundaries).

## Public API
```gdscript
# Key methods and signals exposed to other systems
func example_method(param: Type) -> ReturnType
signal example_signal(data)
```

## Dependencies
- `OtherSystem` — why it's needed

## Data
What CSVs or Resources this system reads. What it writes to GameState.

## Known Issues / TODOs
- [ ] Item

## Last Updated
YYYY-MM-DD
